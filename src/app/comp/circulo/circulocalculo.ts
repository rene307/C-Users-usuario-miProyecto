// circulocalculo.ts
//export function calcularPerimetro(radio: number): number | null {
  //if (radio > 0) {
   // return 2 * Math.PI * radio; // Fórmula para calcular el perímetro
 // }
  //return null; // Retorna null si el radio es inválido
//}

